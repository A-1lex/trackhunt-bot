services:
  music_bot:
    build: .
    container_name: music_bot
    volumes:
      - .:/app
    env_file:
      - .env
    depends_on:
      - sqlite_db

  sqlite_db:
    image: nouchka/sqlite3
    container_name: sqlite_db
    stdin_open: true
    tty: true
